# Copy to .env (git-ignored, never committed). Read by book_studio.py and discover.py.
# Web portal login for iPlus Living (app.iplusliving.com). The mobile app's SMS OTP is separate;
# the web portal uses username + password and the script logs in with these every run.
IPLUS_URL=https://app.iplusliving.com/site/login
IPLUS_USERNAME=
IPLUS_PASSWORD=
